import 'package:flutter/material.dart';

class Electriciter extends StatelessWidget {
  const Electriciter({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    //print(ann.fullname);
    return SingleChildScrollView(
      child: Column(
        children: const [
          Text("elect")
        ],
      ),
    );
  }
}